# fly.toml file generated for customer-dashboard-pro on 2022-09-26T16:02:15+05:30

app = "customer-dashboard-pro"
kill_signal = "SIGINT"
kill_timeout = 5
processes = []

[env]
	PORT = "8080"
  HOST = "customer-dashboard-pro.fly.dev"
  SHOPIFY_API_KEY = "4739402e245941dc78b7d370ad8fcf8f"
  SCOPES = "read_products,read_customers,read_orders,read_locales,read_content,read_script_tags,read_themes,write_themes,write_script_tags,write_products,write_customers,write_orders"

[experimental]
  allowed_public_ports = []
  auto_rollback = true

[[services]]
  http_checks = []
  internal_port = 8080
  processes = ["app"]
  protocol = "tcp"
  script_checks = []
  [services.concurrency]
    hard_limit = 25
    soft_limit = 20
    type = "connections"

  [[services.ports]]
    force_https = true
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

  [[services.tcp_checks]]
    grace_period = "1s"
    interval = "15s"
    restart_limit = 0
    timeout = "2s"
